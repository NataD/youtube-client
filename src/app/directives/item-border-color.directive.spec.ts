import { ItemBorderColorDirective } from './item-border-color.directive';

describe('ItemBorderColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemBorderColorDirective();
    expect(directive).toBeTruthy();
  });
});
